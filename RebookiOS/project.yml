name: RebookiOS
options:
  bundleIdPrefix: com.rebook
targets:
  RebookiOS:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources: [Sources]
    info:
      path: Info.plist
      properties:
        UILaunchScreen: {}
